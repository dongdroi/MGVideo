import React from 'react-native';
//var RCTNetworkUtils = require('NativeModules').NetworkModule;

const {
  Component,
  StyleSheet,
  requireNativeComponent,
  PropTypes,
  NativeModules,
  View,
} = React;

var iface = {
  name: 'MGVideo',
  propTypes: {
    videoPath: PropTypes.string,
    source: PropTypes.object,
    resizeMode: PropTypes.string,
    stopped: PropTypes.bool,
    onPrepared: PropTypes.func,
    ...View.propTypes,
  },
};

module.exports = requireNativeComponent('RCTMGVideo', iface);