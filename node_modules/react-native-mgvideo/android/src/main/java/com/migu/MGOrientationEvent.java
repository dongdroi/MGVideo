package com.migu;

import com.facebook.react.bridge.Arguments;
import com.facebook.react.bridge.WritableMap;
import com.facebook.react.uimanager.events.Event;
import com.facebook.react.uimanager.events.RCTEventEmitter;

class MGOrientationEvent extends Event<MGOrientationEvent> {

  public static final String EVENT_NAME = "orientationChanged";

  private final int mOrientation;
  
  protected MGOrientationEvent(int viewTag, long timestampMs, int orientation) {
    super(viewTag, timestampMs);
    mOrientation = orientation;
  }

  @Override
  public String getEventName() {
    return EVENT_NAME;
  }

  @Override
  public void dispatch(RCTEventEmitter rctEventEmitter) {
    rctEventEmitter.receiveEvent(getViewTag(), getEventName(), serializeEventData());
  }

  private WritableMap serializeEventData() {
    WritableMap eventData = Arguments.createMap();
    eventData.putInt("orientation", mOrientation);
    return eventData;
  }
}
